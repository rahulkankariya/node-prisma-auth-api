#PROJECT NODEJS -ORM -PRISMA -POSTGRESQL -TYPESCRIPT -EXPRESS  -JWT -BCRYPT -JOI

#FIRST STEP
npm install

# if create database manually then skip below step otehrwise follow below step

npx prisma migrate dev --name init


# if manually table geneater othewise 
  npx prisma generate   


got run PROJECT

npm run dev



.env fil values


DATABASE_URL="postgresql://postgres:123456@localhost:5432/mydb?schema=public"

JWT_SECRET="1234567890abcdef"
JWT_EXPIRES_IN=604800
BCRYPT_SALT=10
EMAIL_USER="email_id"
EMAIL_PASS="app_password"
PORT=5000


